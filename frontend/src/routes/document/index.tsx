import { FC } from "react";

import MainLayout from "../../layouts/MainLayout";

const Document: FC = () => {
  return (
    <MainLayout>
      "Document Upload"
    </MainLayout>
  );
};

export default Document;
